<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.epfl.sweng.SDP"
    android:screenOrientation="portrait">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".game.WaitingPageActivity" />
        <activity android:name=".game.drawing.DrawingOffline" />
        <activity android:name=".game.drawing.DrawingOnline" />
        <activity android:name=".game.drawing.DrawingActivity" />
        <activity android:name=".auth.AccountCreationActivity" />
        <activity android:name=".game.VotingPageActivity" />
        <activity android:name=".home.LeaderboardActivity" />
        <activity android:name=".home.LeaguesActivity" />
        <activity android:name=".game.LoadingScreenActivity" />
    </application>

</manifest>

